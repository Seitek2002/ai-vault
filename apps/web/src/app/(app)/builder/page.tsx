import { ConstructorClient } from "@/components/constructor/ConstructorClient";

export const metadata = { title: "Конструктор шаблонов — Vault" };

export default function ConstructorPage() {
  return <ConstructorClient />;
}
