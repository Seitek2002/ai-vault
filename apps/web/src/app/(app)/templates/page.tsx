import { TemplatesClient } from "@/components/templates/TemplatesClient";

export const metadata = { title: "Шаблоны — AI Vault" };

export default function TemplatesPage() {
  return <TemplatesClient />;
}
